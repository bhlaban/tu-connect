<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TUConnect.Mobile.ViewModels"
             x:Class="TUConnect.Mobile.Views.MembersPage"
             x:DataType="viewmodels:MemberViewModel"
             Title="Members">

    <Grid RowDefinitions="Auto,*,Auto" Padding="10">
        <!-- Header -->
        <Button
            Grid.Row="0"
            Text="Add Member"
            Command="{Binding AddMemberCommand}"
            HorizontalOptions="End"
            Margin="0,10" />

        <!-- List -->
        <CollectionView
            Grid.Row="1"
            ItemsSource="{Binding Members}"
            SelectionMode="Single"
            SelectedItem="{Binding SelectedMember}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem
                                    Text="Delete"
                                    BackgroundColor="Red"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:MemberViewModel}}, Path=DeleteMemberCommand}"
                                    CommandParameter="{Binding .}" />
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <Frame Padding="10" Margin="5" BorderColor="LightGray">
                            <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto,Auto,Auto">
                                <Label
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Text="{Binding FirstName}"
                                    FontAttributes="Bold"
                                    FontSize="18" />
                                <Label
                                    Grid.Row="0"
                                    Grid.Column="1"
                                    Text="{Binding LastName}"
                                    FontAttributes="Bold"
                                    FontSize="18" />
                                <Label
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="2"
                                    Text="{Binding Email}"
                                    FontSize="14"
                                    TextColor="Gray" />
                                <Label
                                    Grid.Row="2"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="2"
                                    Text="{Binding JoinDate, StringFormat='Member since: {0:MMM dd, yyyy}'}"
                                    FontSize="12"
                                    TextColor="Gray" />
                            </Grid>
                        </Frame>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Loading Indicator -->
        <ActivityIndicator
            Grid.Row="1"
            IsRunning="{Binding IsLoading}"
            IsVisible="{Binding IsLoading}"
            HorizontalOptions="Center"
            VerticalOptions="Center" />

        <!-- Load Button -->
        <Button
            Grid.Row="2"
            Text="Refresh"
            Command="{Binding LoadMembersCommand}"
            HorizontalOptions="Center"
            Margin="0,10" />
    </Grid>

</ContentPage>
